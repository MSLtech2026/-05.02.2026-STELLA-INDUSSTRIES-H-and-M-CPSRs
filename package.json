{
  "name": "formula-importer",
  "version": "1.0.0",
  "description": "Automates bulk import of product formulations into MySwissLab+ via browser automation",
  "main": "src/import-formulas.js",
  "scripts": {
    "import": "node src/import-formulas.js",
    "import:headless": "node src/import-formulas.js --headless",
    "capture-api": "node src/capture-api.js"
  },
  "dependencies": {
    "csv-parse": "^5.5.6",
    "playwright": "^1.44.0",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
